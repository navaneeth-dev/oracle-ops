apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base

patches:
  # Replace all volsync secrets with dev version
  - path: dev-secrets/volsync.yaml
    target:
      group: ""
      version: v1
      kind: Secret
      name: .*-volsync
